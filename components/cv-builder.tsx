"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { analyzeCv } from "@/lib/analysis";

type CvData = {
  name: string; surname: string; role: string; email: string; phone: string; city: string; linkedin: string; portfolio: string; profile: string;
  experiences: string[]; education: string[]; skills: string[]; languages: string; certifications: string; courses: string; licence: string; availability: string; privacy: boolean;
};

const empty: CvData = { name: "", surname: "", role: "", email: "", phone: "", city: "", linkedin: "", portfolio: "", profile: "", experiences: [""], education: [""], skills: [""], languages: "", certifications: "", courses: "", licence: "", availability: "", privacy: true };

export function CvBuilder() {
  const [data, setData] = useState<CvData>(empty); const [ready, setReady] = useState(false); const [saved, setSaved] = useState(false);
  useEffect(() => { queueMicrotask(() => { try { const value = localStorage.getItem("creailcv-data"); if (value) setData({ ...empty, ...JSON.parse(value) }); } catch {} setReady(true); }); }, []);
  useEffect(() => {
    if (!ready) return;
    try { localStorage.setItem("creailcv-data", JSON.stringify(data)); queueMicrotask(() => setSaved(true)); }
    catch { queueMicrotask(() => setSaved(false)); }
    const timer = window.setTimeout(() => setSaved(false), 1800);
    return () => window.clearTimeout(timer);
  }, [data, ready]);
  const text = useMemo(() => cvText(data), [data]); const ats = analyzeCv(text);
  const required = [data.name, data.surname, data.role, data.email, data.phone, data.city, data.profile, data.experiences.some(Boolean), data.education.some(Boolean), data.skills.filter(Boolean).length >= 3, data.languages, data.privacy];
  const completeness = Math.round(required.filter(Boolean).length / required.length * 100);
  const set = <K extends keyof CvData>(key: K, value: CvData[K]) => setData({ ...data, [key]: value });
  const updateList = (key: "experiences" | "education" | "skills", index: number, value: string) => set(key, data[key].map((item, i) => i === index ? value : item));
  const add = (key: "experiences" | "education" | "skills") => set(key, [...data[key], ""]);
  const exportPdf = async () => {
    const { jsPDF } = await import("jspdf"); const doc = new jsPDF({ unit: "mm", format: "a4" }); const margin = 18; let y = 20;
    doc.setTextColor(21,37,36); doc.setFont("helvetica", "bold"); doc.setFontSize(22); doc.text(`${data.name || "Nome"} ${data.surname || "Cognome"}`, margin, y); y += 8;
    doc.setTextColor(23,107,77); doc.setFontSize(12); doc.text(data.role || "Ruolo professionale", margin, y); y += 8; doc.setTextColor(70,85,80); doc.setFont("helvetica", "normal"); doc.setFontSize(9); doc.text([data.email, data.phone, data.city, data.linkedin, data.portfolio].filter(Boolean).join("  |  "), margin, y, { maxWidth: 174 }); y += 10;
    const addSection = (title: string, content: string) => { if (!content.trim()) return; const lines = doc.splitTextToSize(content, 174); const requiredHeight = 10 + lines.length * 4.5; if (y + requiredHeight > 278) { doc.addPage(); y = 20; } doc.setTextColor(23,107,77); doc.setFont("helvetica", "bold"); doc.setFontSize(10); doc.text(title.toUpperCase(), margin, y); y += 6; doc.setTextColor(45,58,54); doc.setFont("helvetica", "normal"); doc.setFontSize(9.5); doc.text(lines, margin, y); y += lines.length * 4.5 + 6; };
    addSection("Profilo professionale", data.profile); addSection("Esperienze lavorative", data.experiences.filter(Boolean).join("\n\n")); addSection("Istruzione e formazione", data.education.filter(Boolean).join("\n\n")); addSection("Competenze", data.skills.filter(Boolean).join(" • ")); addSection("Lingue", data.languages); addSection("Certificazioni", data.certifications); addSection("Corsi", data.courses); addSection("Patente", data.licence); addSection("Disponibilità", data.availability); if (data.privacy) addSection("Privacy", "Autorizzo il trattamento dei dati personali contenuti nel presente curriculum ai sensi del Regolamento UE 2016/679.");
    const filename = `${data.name || "nome"}-${data.surname || "cognome"}-${data.role || "cv"}`.toLowerCase().replace(/[^a-z0-9à-ù]+/gi, "-").replace(/^-|-$/g, ""); doc.save(`${filename}.pdf`);
  };
  return <div><div className="mb-5 flex flex-col gap-3 rounded-2xl border border-[#dfe5df] bg-white p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-6"><Metric label="Completezza" value={completeness} /><Metric label="Punteggio ATS" value={ats.score} /></div><span aria-live="polite" className="text-xs font-bold text-[#65736f]">{saved ? "Salvato nel browser" : "Salvataggio automatico attivo"}</span></div><div className="grid items-start gap-6 xl:grid-cols-[.95fr_1.05fr]"><div className="card p-4 sm:p-6"><Section title="Informazioni personali"><div className="grid gap-4 sm:grid-cols-2"><Input label="Nome" value={data.name} onChange={(x) => set("name", x)} /><Input label="Cognome" value={data.surname} onChange={(x) => set("surname", x)} /><Input label="Professione o ruolo desiderato" value={data.role} onChange={(x) => set("role", x)} wide /><Input label="Email" type="email" value={data.email} onChange={(x) => set("email", x)} /><Input label="Telefono" value={data.phone} onChange={(x) => set("phone", x)} /><Input label="Città" value={data.city} onChange={(x) => set("city", x)} /><Input label="LinkedIn" value={data.linkedin} onChange={(x) => set("linkedin", x)} /><Input label="Portfolio" value={data.portfolio} onChange={(x) => set("portfolio", x)} /></div></Section><Section title="Profilo professionale"><Text label="Riassumi esperienza, specializzazione e contributo" value={data.profile} onChange={(x) => set("profile", x)} /></Section><ListSection title="Esperienze lavorative" items={data.experiences} placeholder="Ruolo · Azienda · Date\nDescrivi attività, responsabilità e risultati verificabili." onChange={(i,x) => updateList("experiences",i,x)} onAdd={() => add("experiences")} addLabel="Aggiungi esperienza" /><ListSection title="Istruzione" items={data.education} placeholder="Titolo · Istituto · Anno\nAggiungi dettagli pertinenti al ruolo." onChange={(i,x) => updateList("education",i,x)} onAdd={() => add("education")} addLabel="Aggiungi istruzione" /><ListSection title="Competenze" items={data.skills} placeholder="Esempio: Excel - tabelle pivot" onChange={(i,x) => updateList("skills",i,x)} onAdd={() => add("skills")} addLabel="Aggiungi competenza" compact /><Section title="Altre informazioni"><div className="grid gap-4 sm:grid-cols-2"><Text label="Lingue" value={data.languages} onChange={(x) => set("languages", x)} small /><Text label="Certificazioni" value={data.certifications} onChange={(x) => set("certifications", x)} small /><Text label="Corsi" value={data.courses} onChange={(x) => set("courses", x)} small /><Text label="Patente" value={data.licence} onChange={(x) => set("licence", x)} small /><Text label="Disponibilità" value={data.availability} onChange={(x) => set("availability", x)} small /><label className="flex items-start gap-3 rounded-xl border border-[#dfe5df] p-4 text-sm font-bold"><input type="checkbox" className="mt-1 accent-[#176b4d]" checked={data.privacy} onChange={(e) => set("privacy", e.target.checked)} />Autorizzazione al trattamento dei dati personali</label></div></Section></div><div className="xl:sticky xl:top-24"><CvPreview data={data} /><div className="no-print mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><button onClick={exportPdf} className="btn btn-primary col-span-2 sm:col-span-1">Scarica PDF</button><Link className="btn btn-secondary" href="/strumenti/controllo-cv-ats">Controlla ATS</Link><Link className="btn btn-secondary" href="/strumenti/generatore-lettera-presentazione">Genera lettera</Link><Link className="btn btn-secondary" href="/strumenti/email-candidatura">Genera email</Link></div><p className="mt-3 text-xs leading-5 text-[#65736f]">Il PDF usa un formato essenziale e testuale. Controlla sempre il file scaricato prima di inviarlo.</p></div></div></div>;
}

export function CvPreview({ data }: { data: CvData }) {
  return <article className="mx-auto aspect-[210/297] min-h-[620px] w-full max-w-[720px] overflow-hidden bg-white p-[7%] shadow-[0_18px_60px_rgba(18,43,54,.12)]"><header className="border-b-2 border-[#176b4d] pb-5"><h2 className="text-2xl font-black tracking-tight sm:text-3xl">{data.name || "Nome"} {data.surname || "Cognome"}</h2><p className="mt-1 font-bold text-[#176b4d]">{data.role || "Ruolo professionale"}</p><p className="mt-3 break-words text-[10px] leading-5 text-[#5e6c69]">{[data.email || "email@esempio.it", data.phone || "+39 000 0000000", data.city || "Città", data.linkedin, data.portfolio].filter(Boolean).join(" · ")}</p></header><PreviewSection title="Profilo professionale" content={data.profile || "Aggiungi un profilo professionale breve e specifico per presentare esperienza, competenze e obiettivo."} /><PreviewSection title="Esperienze lavorative" items={data.experiences} empty="Le tue esperienze appariranno qui." /><PreviewSection title="Istruzione e formazione" items={data.education} empty="Il tuo percorso di studi apparirà qui." /><PreviewSection title="Competenze" items={data.skills} inline empty="Aggiungi almeno cinque competenze pertinenti." />{data.languages && <PreviewSection title="Lingue" content={data.languages} />}{data.certifications && <PreviewSection title="Certificazioni" content={data.certifications} />}{data.privacy && <p className="mt-7 text-[8px] leading-4 text-[#71807a]">Autorizzo il trattamento dei dati personali contenuti nel presente curriculum ai sensi del Regolamento UE 2016/679.</p>}</article>;
}

function cvText(d: CvData) { return `PROFILO PROFESSIONALE\n${d.profile}\nESPERIENZE LAVORATIVE\n${d.experiences.join("\n")}\nISTRUZIONE E FORMAZIONE\n${d.education.join("\n")}\nCOMPETENZE\n${d.skills.join(", ")}\nLINGUE\n${d.languages}\n${d.email} ${d.phone}\n${d.privacy ? "Autorizzo il trattamento dei dati personali ai sensi del Regolamento UE 2016/679" : ""}`; }
function Metric({ label, value }: { label: string; value: number }) { return <div><span className="text-xs font-bold text-[#65736f]">{label}</span><p className="text-xl font-black text-[#176b4d]">{value}%</p></div>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className="border-b border-[#e1e6e2] py-6 first:pt-0 last:border-0"><h2 className="mb-4 text-lg font-black">{title}</h2>{children}</section>; }
function Input({ label, value, onChange, type = "text", wide = false }: { label: string; value: string; onChange: (x:string) => void; type?: string; wide?: boolean }) { return <label className={`label ${wide ? "sm:col-span-2" : ""}`}>{label}<input type={type} className="input" value={value} onChange={(e) => onChange(e.target.value)} /></label>; }
function Text({ label, value, onChange, small = false }: { label: string; value: string; onChange: (x:string) => void; small?: boolean }) { return <label className="label">{label}<textarea className={`input resize-y ${small ? "min-h-24" : "min-h-32"}`} value={value} onChange={(e) => onChange(e.target.value)} /></label>; }
function ListSection({ title, items, placeholder, onChange, onAdd, addLabel, compact = false }: { title:string; items:string[]; placeholder:string; onChange:(i:number,x:string)=>void; onAdd:()=>void; addLabel:string; compact?:boolean }) { return <Section title={title}><div className="grid gap-3">{items.map((item,i) => <textarea aria-label={`${title} ${i+1}`} key={i} className={`input resize-y ${compact ? "min-h-12" : "min-h-28"}`} placeholder={placeholder} value={item} onChange={(e) => onChange(i,e.target.value)} />)}</div><button className="btn btn-quiet mt-3 !min-h-10 !py-2 text-sm" onClick={onAdd}>+ {addLabel}</button></Section>; }
function PreviewSection({ title, content, items, inline = false, empty }: { title:string; content?:string; items?:string[]; inline?:boolean; empty?:string }) { const values = items?.filter(Boolean) ?? []; return <section className="mt-6"><h3 className="text-[9px] font-black uppercase tracking-[.14em] text-[#176b4d]">{title}</h3>{content && <p className="mt-2 whitespace-pre-line text-[10px] leading-[1.6] text-[#384844]">{content}</p>}{items && (values.length ? <div className={inline ? "mt-2 flex flex-wrap gap-1.5" : "mt-2 grid gap-3"}>{values.map((item,i) => inline ? <span className="rounded bg-[#edf2ee] px-2 py-1 text-[9px] font-bold" key={i}>{item}</span> : <p className="whitespace-pre-line text-[10px] leading-[1.55] text-[#384844]" key={i}>{item}</p>)}</div> : <p className="mt-2 text-[10px] italic text-[#89938f]">{empty}</p>)}</section>; }
